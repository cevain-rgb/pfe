# 1. creer le repertoire du projet
# mkdir projet_pfe
# cd projet_pfe
# puis copier ce fichier(requirements.txt) dans projet_pfe

# 2. creer un environnement virtuelle
#  python3 -m venv .env (linux)ou 
#  python -m venv .env (windows)

# 3. activation de l'environnement
# . .env/bin/activate  (linux)ou
# . .env/script/activate(windows)

# 4. installation des modules
# pip install -r requirements.txt
opencv-python>=4.8
mediapipe>=0.10
tensorflow>=2.12
ultralytics>=8.0
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
flask>=3.0